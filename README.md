# knarrlab-scripts

Small, reusable shell scripts used across my machines.

## Usage

Add `bin/` to your PATH or symlink individual scripts.

## Installation

This repository does not install anything automatically.

An example installer template is provided in `install.example.sh`.
Copy and adapt it to your own environment if desired.

